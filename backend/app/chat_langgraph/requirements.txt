# app/chat_langgraph/requirements.txt
# Dependências específicas do módulo Chat LangGraph
# 
# INSTALAÇÃO:
#   pip install -r app/chat_langgraph/requirements.txt
#
# NOTA: Algumas dependências podem já estar no projeto principal.
#       Instale apenas as que estão faltando.

# === LangGraph (Core) ===
langgraph>=0.2.0
langchain-core>=0.3.0

# === Persistência de Estado ===
# PostgresSaver para manter estado entre mensagens
langgraph-checkpoint-postgres>=0.0.6

# === HTTP Client ===
# Para chamadas à API Groq e Kestra
httpx>=0.27.0

# === Validação ===
# Já deve estar instalado (FastAPI depende)
pydantic>=2.0.0

# === Utilitários ===
python-dateutil>=2.8.0
